/**
 * 强大的splice(start, count, item1, item2) 
 * 删除现有的元素 或者添加新元素来更改一个数组的内容
 */

array.splice(start); // 删除start后面所有的元素 包括start
array.splice(start, 0, item); // 在start后面插入元素
array.splice(start, 1); // 从start位置开始删除一个元素


/**
 * push     在数组末尾添加新的元素 返回数组长度。改变数组内容
 * pop      删除数组的最后一个元素 返回该元素的值 更改数组
 * shift    删除数组的第一个元素 返回该元素的值 更改数组 
 * unshift  在数组的第一位添加新元素 返回新数组的长度
 */
 
 

